node_modules
.vscode
target
*.scale
Cargo.lock
.DS_Store
.scratch/
site

# Python
venv
__pycache__
scripts/__pycache__/*

# MkDocs social cards
.cache/

# Vale rules and the style guide vocab are VENDORED (committed) into
# styles/StyleGuide/ and styles/config/vocabularies/StyleGuide/ so linting is
# self-contained. Refresh from upstream with sync-styleguide-vale.sh.

# Claude
.claude/scheduled_tasks.lock
